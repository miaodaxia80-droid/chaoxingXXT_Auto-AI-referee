/**
 * 环境配置
 *
 * 微信小程序：直连后端（开发期在开发者工具勾选「不校验合法域名」；
 * 真机预览把 IP 换成本机局域网 IP；体验版/正式版换成 HTTPS 域名）。
 *
 * H5：使用相对路径同源访问，开发期由 vite 代理 /api 到本机后端，
 * 生产期与后端同域部署（同一反向代理下）。
 */
let base = 'http://127.0.0.1:5002'
// #ifdef H5
base = ''
// #endif
export const API_BASE = base
